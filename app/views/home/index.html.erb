<div data-controller="home-login" class="text-md font-medium text-gray-900 text-center">
    <div class="pt-4">
        <h4 class="text-2xl leading-6 text-gray-900 mt-2 font-medium">Welcome to Motainai Admin</h4>
        <p class="mt-2">
            <% if is_approved_user %>
                You are signed in. Lets have a think about what contant we want to show here, eh?
            <% elsif user_signed_in? %>
                Your account is pending approval. Please contact your administrator.
            <% else %>
                Please <a data-home-login-target="userAuthLink" class="text-teal-500 hover:text-teal-300 focus:underline cursor-pointer">sign up</a> or <a data-home-login-target="userAuthLink" class="text-teal-500 hover:text-teal-300 focus:underline cursor-pointer">log in</a>.
            <% end %>
        </p>
    </div>
</div>

<%= render 'shared/modal', modal_content: 'home/user_auth_form' %>

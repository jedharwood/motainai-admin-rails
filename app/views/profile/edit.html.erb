<div class="text-md font-medium text-gray-900 text-center">
    <div class="p-4">
        <h4 class="text-2xl leading-6 text-gray-900 mt-2">Update profile</h4>
        <div class="mt-2">
            <p class="font-normal">Your login has been successfully created. Please update your profile information. You will be contacted by an administrator once your account is approved</p>
            <%= form_with model: @profile, url: edit_profile_path, method: :patch, local: true, scope: :profile do |f| %>
                <div class="w-full sm:w-1/2 inline-block mt-4">
                    <div class="shadow-sm -space-y-px focus:z-10">
                        <div>
                            <%= f.label :first_name, "First name", class: "sr-only" %>
                            <%= f.text_field :first_name, autofocus: true, placeholder: "First name", class: "appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 rounded-md focus-within:ring-1 focus-within:ring-slate-800 focus-within:border-slate-800 sm:text-sm mb-4" %>
                        </div>
                        <div>
                            <%= f.label :last_name, "Last name", class: "sr-only" %>
                            <%= f.text_field :last_name, autofocus: true, placeholder: "Last name", class: "appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 rounded-md focus-within:ring-1 focus-within:ring-slate-800 focus-within:border-slate-800 sm:text-sm mb-4" %>
                        </div>
                    </div>
                    <div>
                        <%= f.submit "Update", class: "group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-teal-500 hover:bg-teal-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-slate-800" %>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>